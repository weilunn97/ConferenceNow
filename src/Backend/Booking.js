class Booking {
    constructor(user, room, date, timeslot) {
        this.user = user;
        this.room = room;
        this.date = date;
        this.timeslot = timeslot;
    }
}

export default Booking
